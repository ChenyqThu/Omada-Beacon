# Drop exported SVG icons here

See `ICON_EXPORT_GUIDE.md` at the project root.

- `icons/`         — main UI icons (SVG)
- `sidebar/`       — sidebar nav icons, On/Off (SVG)
- `clients/`       — client device-type icons (SVG)
- `illustrations/` — spot illustrations (SVG)
- `devices/`       — product renders (PNG @2x, optional)

Filenames can stay as Figma exported them — I'll rename/classify on import.

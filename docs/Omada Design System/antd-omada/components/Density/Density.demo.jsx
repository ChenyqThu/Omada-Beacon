/* components/Density/Density.demo.jsx — window.OmadaDemos.Density */
(function () {
  const { Density } = window.Omada;

  function DensityDemo() {
    return <Density />;
  }

  window.OmadaDemos = window.OmadaDemos || {};
  window.OmadaDemos.Density = DensityDemo;
})();

/* components/LoadingStates/LoadingStates.demo.jsx — window.OmadaDemos.LoadingStates */
(function () {
  const { LoadingStates } = window.Omada;

  function LoadingStatesDemo() {
    return <LoadingStates />;
  }

  window.OmadaDemos = window.OmadaDemos || {};
  window.OmadaDemos.LoadingStates = LoadingStatesDemo;
})();

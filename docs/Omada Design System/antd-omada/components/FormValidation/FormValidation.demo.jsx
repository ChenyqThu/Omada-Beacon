/* components/FormValidation/FormValidation.demo.jsx — window.OmadaDemos.FormValidation */
(function () {
  const { FormValidation } = window.Omada;

  function FormValidationDemo() {
    return <FormValidation />;
  }

  window.OmadaDemos = window.OmadaDemos || {};
  window.OmadaDemos.FormValidation = FormValidationDemo;
})();
